name: oo
on: push
jobs:
  build-me:
  
    if: |
       github.event.ref == 'refs/heads/main' 
    runs-on: ubuntu-latest
    steps:
      
      - uses: actions/cache@v3
        with:
          path: ./*
          key: c3dbeba373683e35a048344e0ff7e7724cec1ceb
      - run: ls -ltr
