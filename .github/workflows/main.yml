name: main

on:
  workflow_dispatch:
  
jobs:
  inputs:
    uses: sark1/Actions/.github/workflows/pipelineconfig.yml@main
    
  compile:
    needs: [inputs] 
    if: ${{ (needs.inputs.outputs.build_tool == 'maven') && (needs.inputs.outputs.language == 'java8') }}
    uses: sark1/Actions/.github/workflows/java1_8-maven.yml@main
   
  

  
        
 
